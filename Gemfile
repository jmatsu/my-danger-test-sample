# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'danger-checkstyle_reports', '0.0.1.pre.rc01' #, path: '../danger-checkstyle_reports'

gem 'pry'